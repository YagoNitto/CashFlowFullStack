@page "/login"
@layout HeadlessLayout
@inherits LoginPage

<PageTitle>Entrar</PageTitle>

<MudGrid Justify="Justify.Center">
    <MudItem xs="12" sm="12" md="8" lg="6">
        <MudPaper Class="mud-width-full pa-8">
            <div class="d-flex justify-center">
                <img src="imgs/dima.png" alt="Dima controle financeiro" width="200" />
            </div>

            <EditForm Model="@InputModel" OnValidSubmit="OnValidSubmitAsync">
                <DataAnnotationsValidator />
                
                <MudText Class="d-flex justify-center mt-5" Typo="Typo.h5">Entrar</MudText>
                
                <MudTextField
                    T="string"
                    Label="E-mail"
                    HelperText="Campo Obrigatório"
                    InputType="InputType.Email"
                    For="@(() => InputModel.Email)"
                    @bind-Value="InputModel.Email" />

                <MudTextField
                    T="string"
                    Label="Senha"
                    HelperText="Campo Obrigatório"
                    InputType="InputType.Password"
                    For="@(() => InputModel.Password)"
                    @bind-Value="InputModel.Password" />

                <div class="d-flex mt-8">
                    @if (IsBusy)
                    {
                        <MudProgressCircular Color="Color.Info" Indeterminate="true" />
                    }
                    else
                    {
                        <MudButton ButtonType="ButtonType.Submit" Variant="Variant.Filled" Color="Color.Primary">Entrar</MudButton>
                        <MudSpacer/>
                        <MudButton Href="/registrar">Quero me cadastrar</MudButton>
                    }
                </div>
            </EditForm>
        </MudPaper>
    </MudItem>
</MudGrid>
